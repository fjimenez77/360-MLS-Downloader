# 360 MLS Downloader — Session Context

> Single source of truth for "where are we right now?" Read first every session.
> ⚠️ Auto-generated by `//onboard --resume` on 2026-05-17 — **HUMAN REVIEW REQUIRED** on every section.

## Project snapshot

| Field | Value |
|---|---|
| Name | 360 MLS Downloader |
| Version | 2.0.0 (see `VERSION`) |
| Scale | LITE |
| Maintainer | PRHack \| CyberSpartan77 (@fjimenez77) |
| License | Apache-2.0 |
| Primary language | Python 3.10+ (tested on 3.14) |
| Runtime deps | `requests` (hard) · `playwright` (Zillow only, auto-installed) |
| Architecture | Modular monolith + provider registry (`providers/`) |
| Default branch | `main` |
| Repo | https://github.com/fjimenez77/360-MLS-Downloader |

## Brain configuration

```yaml
brain:
  mode: global              # Personal — lives in ~/.devforge-brain/
  attribution: stealth      # No AI mentions in commits
```

## Active task

`//onboard --resume` just completed (TIER 2 path: A → C → 13). The 6 LITE-essential
`devforge/` docs are scaffolded. Phase B (migrate) was a no-op — there were no
session-state docs to retire (README and CONTRIBUTING are user-facing reference).

## Recent work (since v2.0.0 ship)

Six commits on `main` (newest first):

1. `95caa49` chore: gitignore devforge/ resume state + onboard backup paths
2. `94eff10` fix(viewer): clickable splash page — copy buttons + tour URL link
3. `2bfe7a3` fix(menu): handle PEP 668 externally-managed Python in auto-installer
4. `6194646` feat(menu): add user-confirmed auto-install for missing dependencies
5. `5b18c59` feat(menu): startup dependency check + new 'd' menu option
6. `350385c` security: harden viewer against hostile MLS-supplied JSON

## Next priority

Triage `⚠️ HUMAN REVIEW REQUIRED` entries in this order:

1. `devforge/RISKS.md` — confirm the deferred prior-session items (SEC-003, SEC-004,
   Linux `--no-sandbox`, ToS posture documentation) are still real and prioritized.
2. `devforge/DECISIONS.md` — confirm ADR-001 through ADR-005 are stated correctly.
3. `devforge/CHANGELOG.md` — confirm the auto-extracted v2.0.0 + post-2.0.0 entries.
4. Decide whether to add a CHANGELOG, Dependabot config, or CI workflow.

Then run `//catch-up` next session to load this context automatically.

## Blockers

_None._ Onboard completed cleanly. The previous abort cause (Phase 0.5 context exhaustion)
was resolved by starting a fresh session before running `//onboard --resume`.

## Conventions in this repo

- **Commit style:** `type(scope): subject` — conventional commits with subscope (e.g. `fix(viewer):`, `feat(menu):`, `security:`).
- **Single-dependency policy:** `requests` is the only hard dep. `playwright` is conditional + auto-installed. PRs adding deps will be closed (see ADR-001 + CONTRIBUTING.md).
- **No proxy / CAPTCHA-solver / telemetry egress** (ADR-003 + CONTRIBUTING.md).
- **Human-in-the-loop CAPTCHA is load-bearing** (ADR-002 + CONTRIBUTING.md).
- **No tests yet** — manual verification against real tour URLs per CONTRIBUTING.md.

## Where things live

| Concern | File |
|---|---|
| CLI entry | `mls360-downloader.py` |
| Interactive menu | `mls360-menu.py` |
| Provider-agnostic download engine | `mls360_downloader_core.py` |
| Offline HTML viewer generator | `mls360_viewer.py` |
| Provider registry | `providers/__init__.py` |
| Zillow provider | `providers/zillow.py` |
| Ricoh360 provider | `providers/ricoh360.py` |
| Vendored Pannellum (offline viewer) | `vendor/pannellum.min.{css,js}` |
| User docs | `README.md`, `CONTRIBUTING.md` |
| DevForge state | `devforge/` (this directory) |

---

_Last updated: 2026-05-17 by `//onboard --resume`._
